//----当学习成为习惯，知识变成常识
//1 vuejs 打包构建后有哪些版本呐： 完整版（runtime + complier） + 运行时版本(runtime)
//complier 负责将template选择编译成render函数 
//runtime 负责实例vue，渲染处理虚拟dom，除complier之外的都是运行时代码，如果你引入vue-loader || vueify 会解析.vue文件就不需要complier

// 2.new 初始化的时候做了哪些事情（rollup来打包）

